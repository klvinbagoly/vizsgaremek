import { TextoverflowPipe } from './textoverflow.pipe';

describe('TextoverflowPipe', () => {
  it('create an instance', () => {
    const pipe = new TextoverflowPipe();
    expect(pipe).toBeTruthy();
  });
});
